# InnerGrow.ai 生产环境配置模板
# ⚠️⚠️⚠️  重要安全提醒  ⚠️⚠️⚠️
#
# 🔒 配置文件安全准则：
# 1. 此文件是安全模板，包含示例配置，不包含真实敏感信息
# 2. 复制此文件为 .env.production 并修改为实际配置
# 3. .env.production 文件已在 .gitignore 中保护，不会被提交到版本控制
# 4. 生产环境中确保 .env.production 文件权限为 600 (只有所有者可读写)
# 5. 定期更换密钥和密码，特别是 DJANGO_SECRET_KEY
# 6. 绝不在聊天记录、邮件或文档中暴露真实配置信息
# 7. 备份配置文件时也要确保安全存储
#
# 🚨 Git 操作风险警告：
# - 删除 .开头的文件前请谨慎检查，它们通常包含重要配置
# - 执行 git clean -fd 前要确认不会误删配置文件
# - .env.production 等文件在 .gitignore 中受保护，但删除操作不受此保护
# - 如果误删配置文件，服务器将无法正常启动
#
# 📋 正确使用步骤：
# 1. cp .env.production.example .env.production
# 2. nano .env.production  # 修改为实际配置值
# 3. chmod 600 .env.production  # 设置安全权限
# 4. 测试配置: python test_production_config.py
# 5. 首次部署前备份此配置文件
#
# 💡 推荐做法：
# - 使用密码管理器存储真实配置值
# - 在服务器上建立配置文件的安全备份机制
# - 定期审查和更新安全配置

# ==================== 核心配置 ====================
# Django环境设置
DJANGO_SETTINGS_MODULE=production_settings
DEBUG=False

# 安全密钥（必须修改为随机字符串）
DJANGO_SECRET_KEY=your-super-secret-key-change-this-in-production

# 允许的主机（替换为你的域名）
ALLOWED_HOSTS=your-domain.com,www.your-domain.com,localhost,127.0.0.1

# ==================== 数据库配置 ====================
# PostgreSQL数据库（推荐生产环境使用）
DATABASE_URL=postgresql://username:password@localhost:5432/innergrow_db

# 或者继续使用SQLite（不推荐生产环境）
# DATABASE_URL=sqlite:///db.sqlite3

# ==================== 安全配置 ====================
# HTTPS设置（如果使用HTTPS）
USE_HTTPS=True

# ==================== 服务器配置 ====================
# 域名设置（用于部署脚本显示正确的访问地址）
DOMAIN_NAME=innergrow.ai
PROTOCOL=https
PORT=

# Gunicorn工作进程数（推荐：CPU核心数 * 2 + 1）
GUNICORN_WORKERS=3

# 绑定地址和端口
BIND_ADDRESS=0.0.0.0:8000

# 超时时间（秒）
TIMEOUT=30

# ==================== 缓存配置 ====================
# Redis缓存服务器
REDIS_URL=redis://localhost:6379/0

# ==================== 邮件配置 ====================
# SMTP邮件服务器设置
EMAIL_HOST=smtp.your-email-provider.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@domain.com
EMAIL_HOST_PASSWORD=your-email-password

# ==================== 文件存储 ====================
# 静态文件目录
STATIC_ROOT=/var/www/innergrow/static

# 媒体文件目录
MEDIA_ROOT=/var/www/innergrow/media

# ==================== 日志配置 ====================
# 日志级别
LOG_LEVEL=INFO

# ==================== 监控配置 ====================
# Sentry错误监控（可选）
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# ==================== 其他配置 ====================
# 时区设置
TIME_ZONE=Asia/Shanghai

# 语言设置
LANGUAGE_CODE=zh-hans