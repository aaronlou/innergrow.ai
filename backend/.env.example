# InnerGrow.ai 后端环境配置
# 复制此文件为 .env 并根据实际情况修改配置

# Django配置
DEBUG=True
SECRET_KEY=django-insecure-)ze(7c93_x+38r7v*-$qiv!0=g9*+odq@f==k0km=hxn+@9f$=

# 数据库配置（SQLite默认，生产环境建议使用PostgreSQL）
DATABASE_URL=sqlite:///db.sqlite3

# 允许的主机（生产环境需要设置具体域名）
ALLOWED_HOSTS=localhost,127.0.0.1

# CORS配置（前端地址）
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# 媒体文件配置
MEDIA_ROOT=/Users/lousiyuan/innergrow.ai/backend/media
MEDIA_URL=/media/

# 静态文件配置
STATIC_ROOT=/Users/lousiyuan/innergrow.ai/backend/static
STATIC_URL=/static/

# 邮件配置（可选，用于密码重置等功能）
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=
EMAIL_PORT=
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=

# Redis配置（可选，用于缓存和会话）
REDIS_URL=redis://localhost:6379/0

# 日志级别
LOG_LEVEL=INFO